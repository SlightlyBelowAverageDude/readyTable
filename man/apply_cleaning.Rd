% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functionFile.R
\name{apply_cleaning}
\alias{apply_cleaning}
\title{Apply cleaning actions to selected columns with configurable rules}
\usage{
apply_cleaning(
  df,
  keep_specific_columns = character(),
  change_specific_columns = character(),
  na_threshold = 90,
  drop_constant = TRUE,
  convert_numeric_str = TRUE,
  impute = TRUE,
  impute_method_override = NULL,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data.frame or tibble}

\item{keep_specific_columns}{Character vector of column names that must NOT be changed (default empty)}

\item{change_specific_columns}{Character vector of column names that should be changed (default empty)}

\item{na_threshold}{Percentage threshold of missing data before removal is suggested (default 90)}

\item{drop_constant}{Whether to drop constant columns (default TRUE)}

\item{convert_numeric_str}{Whether to convert numeric-like strings (default TRUE)}

\item{impute}{Whether to impute missing numeric values (default TRUE)}

\item{impute_method_override}{Optional string ("mean" or "median") to force imputation method}

\item{verbose}{Whether to print actions (default TRUE)}
}
\value{
Cleaned data.frame
}
\description{
Apply cleaning actions to selected columns with configurable rules
}
\examples{
#Apply cleaning actions to selected columns
df <- data.frame(
  age = c(25, 30, NA, 45),
  income = c(1000, NA, 2000, 1500),
  const_col = rep("x", 4),
  stringsAsFactors = FALSE
)
#Change only specific columns
cleaned_df <- apply_cleaning(df, change_specific_columns = c("age", "income"))
cleaned_df

#Or keep specific columns untouched
cleaned_df2 <- apply_cleaning(df, keep_specific_columns = c("const_col"))
cleaned_df2
}
